# Switch Kubernetes context with fzf
emulate -L zsh
local ctx
ctx=$(kubectl config get-contexts -o name | fzf --prompt='k8s ctx> ' --height=40%) || return
[[ -n $ctx ]] || return
kubectl config use-context "$ctx"
